# Project Rules (auto-loaded by Cline every session)

## `specs/` folder — approval required before creating files

- Plans and planning documents live under `specs/`.
- Before creating **any new file** under `specs/`, the agent must:
  1. Present the file it is ready to create (path + summary of contents) to the user.
  2. Wait for explicit approval, and only then write the file.
- Never create files under `specs/` without that approval, even if the work itself was already approved.
- This rule was requested by the user on 2026-09-17 and applies to all future sessions.
